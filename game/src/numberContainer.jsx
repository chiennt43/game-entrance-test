import { memo, useState } from "react";

function NumberContainer(props) {
  const {} = props;
  return <div className="numbers">NumberContainer</div>;
}

export default memo(NumberContainer);
